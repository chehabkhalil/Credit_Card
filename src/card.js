import React from 'react';
import './App.css';
class Card {
    constructor(title, puce, serial_number,number,valid_year,valid_month,name_prop,logo,background) {
       this.title=title;
       this.puce=puce;
       this.serial_number=serial_number;
       this.number=number;
       this.valid_year=valid_year;
       this.valid_month=valid_month;
       this.name_prop=name_prop;
       this.logo=logo;
       this.background=background;
        };
    }
;

export default Card;